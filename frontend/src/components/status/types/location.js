import AbstractType from './abstract_type';

export default class Location extends AbstractType {
  static NAME = "Location";
  
  static setterComponent({}) {
    //return (

    //);
  }

  static getterComponent({}) {
    //return (

    //);
  }
}