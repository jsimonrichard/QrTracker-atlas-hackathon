query GetDashboardInfo($userId: String!) {
  trackers(query: {ownerId: $userId}) {
    ...MinFields
  }

  subscriptions(query: {subscriberId: $userId}) {
    tracker {
      ...MinFields
    }
  }

  shared: trackers(query: {collaboratorIds: [$userId]}) {
    ...MinFields
  }
}

fragment MinFields on Tracker {
  title
  status {
    message
  }
  atAGlanceField
  updatedAt
}